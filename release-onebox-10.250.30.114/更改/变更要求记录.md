# 变更要求记录

记录时间：2026-04-24 10:22:15

## 当前约束（由用户确认）

1. 修改范围默认仅限 `frontend/` 与 `backend/`。
2. 部署相关文件（如 `docker-compose.yml`、`deploy/onebox/*`、`start.sh`、`rebuild.sh`、`onebox.env`）默认不改，除非用户明确要求。
3. 从本次记录起，后续对代码文件的修改需添加注释，注释中包含精确到秒的时间戳，格式建议：`YYYY-MM-DD HH:MM:SS`。
4. 当前阶段调试可不启动/不考虑 `agent`，以前后端联调为主。

## 注释规范（执行约定）

- Python: `# [2026-04-24 00:00:00] 变更说明`
- JavaScript: `// [2026-04-24 00:00:00] 变更说明`
- HTML: `<!-- [2026-04-24 00:00:00] 变更说明 -->`

说明：时间戳在实际改动时使用当时系统时间（精确到秒）。
