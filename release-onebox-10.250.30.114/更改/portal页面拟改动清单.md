# portal 页面拟改动清单（待你批阅）

更新时间：2026-04-24 10:45:36

说明：本清单仅列“计划改动”，暂不执行代码修改。你可在每条后面标记“同意/暂缓/拒绝”。

## 一、拟改动文件范围

- `frontend/portal.html`
- `frontend/portal.js`
- `frontend/portal.css`

## 二、改动项（按优先级）

### P0（建议优先）

- [ ] 1. **导航语义化改造（portal.html）**  
  将侧边栏 `div.nav-item` 改为可访问组件（`button` 或 `a`），保留现有样式与 `data-view` 行为，提升键盘可用性与语义。

- [x] 2. **外部 CDN 依赖降级策略（portal.html）**  
  已按批示去除外网依赖（不保留 CDN 引入），避免内网/离线场景失败。

- [x] 3. **隐藏或弱化当前阶段不使用入口（portal.html + portal.js）**  
  已增加开关并默认隐藏 agent 入口（`ENABLE_AGENT_VIEW=false`）。

### P1（建议第二批）

- [x] 4. **统一错误提示交互（portal.js + portal.css）**  
  已将 `alert()` 替换为页面内 toast 轻提示。

- [x] 5. **增加 API 基址可视化与可编辑入口（portal.html + portal.js）**  
  已在顶部增加 API 按钮，可查看并修改 API base，保存到 localStorage。

- [x] 6. **移除临时“页面修复说明”文案（portal.js）**  
  已替换为通用业务说明文案。

### P2（结构治理）

- [x] 7. **清理重复函数定义（portal.js）**  
  已去重 `networkStatusBadgeHtml` 重复定义。

- [ ] 8. **页面信息分组优化（portal.js + portal.css）**  
  服务器卡片中“基础信息/资源指标/维护信息”做结构分组，提升可读性。

## 三、执行约束（与你已确认）

- 仅改 `frontend/`、`backend/`，本次只动 `frontend/`。
- 不修改部署相关文件。
- 每次实际代码修改增加“精确到秒”的时间戳注释：
  - JS: `// [YYYY-MM-DD HH:MM:SS] 说明`
  - HTML: `<!-- [YYYY-MM-DD HH:MM:SS] 说明 -->`
  - CSS: `/* [YYYY-MM-DD HH:MM:SS] 说明 */`

## 四、你的批阅区（可直接在下方写）

- 允许立即执行：
**外部 CDN 依赖降级策略（portal.html）**  
  1.保留现有 CDN 引入，但增加本地兜底机制（或先移除 Google Fonts 依赖），避免内网/离线场景图标与字体异常。---要求：不保留cnd引入，要去除外网依赖。
  2.其它-也全部更改。并做好记录
- 暂缓执行：
- 不执行：

## 五、本轮执行说明

- [2026-04-24 10:45:36] 已按你的最新要求保持侧边栏原有风格，不再进行风格性重构，本轮聚焦逻辑改造。
- [2026-04-24 10:45:14] 根据新批示调整为“agent 入口保留可见”，并置灰+提示“暂未开放”，点击仅提示不进入。
- [2026-04-24 10:51:04] 按方案 A 完成本地化图标依赖：新增 `frontend/vendor/font-awesome/*`，`portal.html` 改为本地 CSS 引用，恢复原 TAB 图标风格且不依赖外网。
- [2026-04-24 10:58:02] 服务器列表逻辑调整：管理IP/宿主机IP 合并为单个 IP 展示；使用人改为“可追加”模式，支持多个使用人（后端逗号分隔存储）。
- [2026-04-24 11:12:56] 服务器列表文案瘦身：移除“离线显示上报”等解释性提示；“使用人（可多个）/当前/输入后点击”等文案改为简洁表达，降低界面噪音。
- [2026-04-24 11:15:25] 服务器列表刷新优化：离线探测循环改为按需刷新，避免每轮多次重绘导致卡片抖动体感。
- [2026-04-24 11:16:57] 服务器列表刷新优化（二次）：去掉探测开始阶段“checking”重绘；重绘判定仅比较状态/消息/延迟，忽略时间戳变化，进一步降低无意义重绘。
- [2026-04-24 11:18:31] 服务器列表刷新优化（三次）：离线探测改为仅局部更新“网络状态徽标”，不再触发整列表重绘；并收紧卡片点击触发范围，避免编辑使用人时误触发重绘。
- [2026-04-24 11:28:51] 服务器列表刷新根治：定位到 `startOfflinePingLoop` 的 `setInterval` 为持续刷新源头，默认关闭离线探测轮询（仅进入页面探测一次）。
- [2026-04-24 11:36:58] 服务器列表刷新根治（二次）：定位到卡片点击链路导致选中文本被打断；已增加“文本选中不切换”和“重复点击已选卡片不重绘”保护。
- [2026-04-24 11:41:37] 按需求增加“动态字段 60s 局部刷新”：网络状态/CPU/内存采用定时局部更新（不整卡片重绘），静态信息不刷新。
- [2026-04-24 11:46:15] 服务器卡片去重展示：当标题服务器名与详情 IP 相同时，隐藏“IP：...”行，避免重复信息。
- [2026-04-24 14:13:58] 网络状态文案升级为“红绿灯图标 + 状态文字”：在线/未接入agent/离线均增加小红绿灯图标，提升可读性与识别速度。
- [2026-04-24 14:16:10] 网络状态细化：将“未接入agent”由红灯调整为黄灯（绿-黄-红语义更清晰）。
- [2026-04-24 14:19:44] 使用人交互增强：改为可删除标签（含小 x），删除前弹确认框，确认后写回后端。
- [2026-04-24 14:24:20] 容器列表新增“使用人”列：展示方式与服务器列表一致为标签化（只读），按服务器 owner_user 映射呈现。
- [2026-04-24 14:31:21] 容器列表列顺序微调：将“使用人”从第 2 列移动到最后（操作前一列），与当前阅读习惯保持一致。
- [2026-04-24 14:34:24] 容器列表列顺序再次修正：将“使用人”调整为真正最后一列（位于“操作”之后）。
- [2026-04-24 14:38:39] 菜单命名与结构调整：`制品库` 更名为 `交付件管理`，新增 `本地镜像库` tab（基于 docker-image 交付件的下载/导入视图）。
- [2026-04-24 14:40:15] 侧边栏 tab 文案清理：移除“服务器列表（V1）”“容器与镜像（V2）”中的版本后缀，保留简洁命名。
- [2026-04-24 14:43:47] 新增 `本地资源导航` tab：聚合 GIT 服务器与数据平台入口（新窗口打开 + 一键复制地址），并补充轻量卡片样式。
- [2026-04-24 14:54:51] 按批示调整：去掉智能助手 `V3`；服务器列表取消整卡点击切换，改为“设为当前环境”按钮；本地资源导航隐藏 URL 文本，改图标化入口。
- [2026-04-24 15:03:50] 本地资源导航视觉微调：移除卡片内新增的 2 个额外图标，回归简洁文字+按钮布局。
- [2026-04-24 15:28:06] 修复服务器列表“仅首张可选”问题：局部刷新网络状态时改为只更新徽标容器，避免覆盖“设为当前环境”按钮。
- [2026-04-24 15:31:45] 交互回调：移除“设为当前环境”按钮，恢复“点击服务器卡片本体选中”，并保留输入区/文本选中防误触保护。
